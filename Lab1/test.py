import numpy as np
import scipy
from config import CONSTS

#TRAPEZOIDAL INTEGRATION EXAMPLE
# a = 0
# b = 1
# n = 10
# x = np.linspace(a, b, n)
# y = [np.sin(2 * np.pi * j) for j in x]
# I = scipy.integrate.trapz(y, x)


print(CONSTS["MyNumber"])
